# Dependências do Analisador Preditivo de Bancas
# Restrição do projeto: só bibliotecas de código aberto, tudo roda local.
# Fase 1 — coleta e extração
requests>=2.32        # download dos PDFs oficiais
pdfplumber>=0.11      # extração de texto e tabelas dos PDFs
pytest>=8.0           # testes das funções frágeis (extração e classificação)
# Fase 2 — classificação local (Camada 2)
scikit-learn>=1.5     # TF-IDF + classificador linear com validação cruzada
# Fase 3 — análise estatística e modelo preditivo
pandas>=2.2           # manipulação tabular das estatísticas
matplotlib>=3.9       # gráficos de frequência por tópico
scipy>=1.14           # intervalos de credibilidade (distribuição Beta)
tabulate>=0.9         # tabelas Markdown nos relatórios (pandas.to_markdown)
# Fase 4 — gerador de plano de estudos
reportlab>=4.0        # exportação do plano em PDF (puro Python, sem GTK)

# Ambiente virtual e caches Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Dados brutos (PDFs grandes) e banco — versionamos código, não dados
data/raw/
data/processed/*.db
data/processed/*.sqlite
# Exceção: o banco final (1,3 MB, derivado e regenerável pelo pipeline)
# é versionado para o Streamlit Community Cloud ter os dados ao clonar.
!data/processed/bancas.db

# Segredos
.env

# Sistema operacional / editor
Thumbs.db
.vscode/
.idea/
